# Internal Docker Services
REDIS_URL=redis://redis:6379
MCP_SERVER_URL=http://mcp-server:8080

# Slack Authentication (Phase 2)
# Get these from https://api.slack.com/apps
# 1. Create a new app at https://api.slack.com/apps
# 2. Enable Socket Mode and get the App-Level Token (SLACK_APP_TOKEN)
# 3. Add Bot Token Scopes: chat:write, commands, app_mentions:read
# 4. Install app to workspace to get Bot User OAuth Token (SLACK_BOT_TOKEN)
# 5. Add Signing Secret from Basic Information page
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_APP_TOKEN=xapp-your-app-token-here
SLACK_SIGNING_SECRET=your-signing-secret-here

# Worker Mode
# TEST_MODE=true  -> Print to console (Phase 1 testing)
# TEST_MODE=false -> Send to Slack webhook (Phase 2 production)
TEST_MODE=true

# Slack Block Kit (Phase 2 - Round 8)
# USE_BLOCKS=true  -> Rich, interactive Slack messages with buttons (recommended)
# USE_BLOCKS=false -> Plain text messages (fallback mode)
USE_BLOCKS=true

# API Keys for External Services
# NIST NVD API Key - Get from: https://nvd.nist.gov/developers/request-an-api-key
# Increases rate limit from 5 requests/30s to 50 requests/30s
NVD_API_KEY=your-nvd-api-key-here

# GitHub API Token - Get from: https://github.com/settings/tokens
# Required for higher rate limits (5000 req/hour vs 60 req/hour)
# Permissions needed: public_repo (read-only)
GITHUB_TOKEN=your-github-token-here
